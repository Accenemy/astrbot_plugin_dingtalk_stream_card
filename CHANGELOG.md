# 更新日志

## 0.2.0

- 新增 `钉钉流式卡片` 平台适配器。
- 支持将 AstrBot 流式 LLM 回复更新到钉钉互动 AI 卡片。
- 支持 `enable_stream_card` 开关，关闭后回退为普通钉钉消息。
- 支持 `card_template_id`、`card_content_key`、`card_update_interval` 配置。
- 支持 `send_normal_reply_as_card` 诊断开关，用于验证卡片模板与钉钉权限。

